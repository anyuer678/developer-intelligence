# v0.1 任务卡 —— 可发布首发版

> 目标：双模式扫描 + SKILL.md + 模板/prompts 冻结稿，达到 Gate 1 发布条件。
> 纪律同 core：每卡独立 commit、勾选回写、决策进 `docs/decisions.md`。

---

## TASK-V01-01 ✅ · 仓库骨架与记录体系 ✅
- 内容: 目录结构 / AGENTS.md / LICENSE(MIT) / 本文件 / decisions

## TASK-V01-02 ✅ · lite 扫描器（stdlib 零依赖）
- 内容: `skill/scripts/scan.py` 内置启发式——语言统计 / manifest / 入口粗判(bin+scripts+main.*) / buildRun(scripts+makefile+生态默认) / 排除(默认清单+.repointelignore) / warnings 含 SCAN_MODE=lite
- 验收命令: `pytest tests/test_lite_scan.py`

## TASK-V01-03 ✅ · 完整模式升级通道
- 内容: find_spec 探测 repo-intel-core → 调 scan_repo 输出全字段；`--mode lite|full|auto` 默认 auto；SCAN_MODE warning 标注实际模式
- 验收命令: `pytest tests/test_backend.py`

## TASK-V01-04 ✅ · 字段契约测试
- 内容: lite 输出键集合 ⊆ Schema v1.0 已知键清单；两模式在同 fixture 上公共字段值一致（core 缺席时 skip）
- 验收命令: `pytest tests/test_contract.py`

## TASK-V01-05 ✅ · SKILL.md + 模板 + prompts 冻结稿
- 内容: frontmatter(name/description) + 五步工作流；PROJECT_GUIDE 七节模板；system/user prompt（含反幻觉铁律与自检清单）
- 验收命令: `pytest tests/test_skill_files.py`

## TASK-V01-06 ✅ · 三宿主接入示例 + README + 收尾
- 内容: README 黄金结构（GIF 位占挂起人工）、dsh/ClaudeCode/opencode 各 4 行接入示例；CHANGELOG；Gate 1 时钟说明
- 验收命令: `pytest && ruff check skill tests`（ruff 存在时）

---

### 明确不做（v0.2 再议）
GitHub URL 直达 / 多语言输出 / Project Memory 姊妹卡 / CI workflow（推远程时补）/ GIF 录制（挂起人工）
