# M1 任务卡 —— 能理解结构

> 目标：入口点识别 + import 解析 → 模块划分 + 内部依赖图；外部依赖清单（runtime/dev）。
> 计划书验收（01 §七 M1）：任选 5 仓库模块划分"说得过去"；依赖图可导出 mermaid。
> 执行纪律：每卡独立 commit；完成后本文件勾选；决策记入 `docs/decisions.md`。

---

## TASK-M1-01 ✅ · 卡片起草与决策日志建立 ✅
- 内容: 本文件 + docs/decisions.md（ADR-lite，格式继承总路线图 §六）

## TASK-M1-02 ✅ · 入口点检测
- 内容: `rules/entrypoints.yaml` 模式表（python-main / fastapi / flask / go-main / node-bin / vue-gui 等）+ `detect/entrypoints.py`
- 验收命令: `pytest tests/test_entrypoints.py`

## TASK-M1-03 ✅ · Python 导入解析
- 内容: `import x.y` / `from a.b import c` / 相对导入 `.mod`；点分名→候选文件路径匹配已扫描集合
- 验收命令: `pytest tests/test_imports_py.py`

## TASK-M1-04 ✅ · JS/TS/Vue 导入解析
- 内容: `import … from 'x'` / `require('x')` / `export … from 'x'`；相对路径解析扩展名候选(.ts/.tsx/.js/.jsx/.vue//index.*)；裸包名(含 @scope/name)标记为 external-import
- 验收命令: `pytest tests/test_imports_js.py`

## TASK-M1-05 ✅ · Go 导入解析
- 内容: 单行/块式 import；go.mod module 名前缀剥离 → 仓库内路径；非前缀导入忽略（外部依赖归 M1-07 的 manifest 口径）
- 验收命令: `pytest tests/test_imports_go.py`

## TASK-M1-06 ✅ · 模块划分与内部依赖图
- 内容: 模块=含代码文件的顶层目录（散根文件归 "(root)"）；文件级解析结果聚合到模块级有向边（weight 计数）；cohesion = 模块内命中 / 该模块全部内部导入；scanner 集成填充 entryPoints/modules/dependencyGraph
- 验收命令: `pytest tests/test_modules_graph.py`

## TASK-M1-07 ✅ · 外部依赖清单
- 内容: 根 manifest 声明口径——package.json(dependencies+devDependencies) / pyproject.toml([project].dependencies) / requirements.txt / go.mod(require)；kind: runtime|dev|runtime(go 全 runtime)；版本取声明原文
- 验收命令: `pytest tests/test_extdeps.py`

## TASK-M1-08 ✅ · CLI `graph` 子命令
- 内容: `repo-intel graph <path> [--format mermaid|json]`；mermaid flowchart LR 渲染模块依赖
- 验收命令: `pytest tests/test_cli_graph.py` + 手动冒烟

## TASK-M1-09 ✅ · 收尾记录
- 内容: 图仓库 fixture 集成回归；AGENTS/CHANGELOG 回写；真实 5 仓库人工确认项挂起为用户动作
- 验收命令: `pytest && ruff check src tests && ruff format --check src tests`

---

### 明确不做（M2 再议）
框架识别规则表 / buildRun 推断 / 复杂度热点 / 测试证据 / tree-sitter AST 增强 /
传递依赖解析 / 社区发现聚类
