# M4 任务卡 —— evocode 下沉批次

> 来源：母目录 `09-evocode对齐审计.md`。按价值排序逐项下沉，每项独立卡与提交。

---

## TASK-M4-01 · 认知复杂度启发式 ✅
- 内容: 移植 evocode complexity_scan 正则版——函数块定位(py 缩进/花括号匹配/go func) + 分支×嵌套惩罚计分，阈值 12 / MAJOR 20 口径原版一致；hotspots 新增 `cognitive-complexity max=N fn=名 over=数` 信号
- 已知特性: 嵌套深度按行首关键字累积（原版行为），阈值校准沿用其经验值；后续若引入 tree-sitter 可精化
- 验收命令: `pytest tests/test_quality.py -k complexity`

## TASK-M4-02 ✅ · pom.xml 依赖解析 + EOL 规则表
- 来源: depscan.py + dep_eol_rules.py
- 要点: extdeps 增加 Maven 口径；dependencies 表/Schema 增 risk 可选列（add-only）
## TASK-M4-03 ✅ · tree-sitter 函数级调用图
- 来源: arch/ 五解析器；含环检测(Tarjan SCC)与分层违规
- 落地: detect/callgraph/{base,parsers,engine}.py；可选依赖 `[arch] extra`（缺 grammar 整体降级 null 三态）；schema 新增 call_graph 块（add-only）；java 纳入文本缓存集合；golden 归一化剥离可选能力输出（CI 无 extra 也绿）
## TASK-M4-04 ✅ · --numstat 全量演化统计
- 来源: gitlog.py；topFiles/authors/周趋势并入月度信号包

### 明确不做
security/style/duplication/bloated 等 12 类体检项——保留在 evocode 平台侧（定位差异，见 09 审计 §五）
