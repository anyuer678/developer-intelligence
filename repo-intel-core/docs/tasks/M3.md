# M3 任务卡 —— 能看见时间

> 目标：git 元数据模块 + 月度切片信号包（03 号 Evolution Timeline 的地基）+ golden 快照基线。
> 计划书验收（01 §七 M3）：03 号项目 M2 开工时本模块就绪。
> 约束澄清：硬约束#3 允许 subprocess 调用**本地只读 git**——无网络、无 LLM。

---

## TASK-M3-01 ✅ · 卡片起草与 Schema 补字段 ✅
- 内容: 本文件；GitMeta 增补 branchCount/tagCount 可选字段（add-only）；ADR-011~013 记入决策日志

## TASK-M3-02 ✅ · GitMeta 模块
- 内容: `gitmeta/reader.py`——单次 `git log` 解析首末提交/总数/贡献者/月度活跃 + `git tag`/branch 计数；无 .git 或命令失败 → null + warning（三态保持）
- 验收命令: `pytest tests/test_gitmeta.py`

## TASK-M3-03 ✅ · 月度信号包
- 内容: `gitmeta/signals.py` 按月切片输出 {commits, contributors, new_dirs, deps_added, top_terms}；依赖变更取月边界间根 manifest diff 的 `+` 行；`repo-intel signals <path> [--months N] [--format summary|json]`
- 验收命令: `pytest tests/test_signals.py` + CLI 冒烟

## TASK-M3-04 ✅ · Golden 快照基线
- 内容: 三个 fixture 仓库的归一化 JSON 快照（剥离 generatedAt/tool.version/repo.path/vcs/git）；`tests/test_golden.py` 对比；`REPO_INTEL_UPDATE_GOLDEN=1` 或 scripts/update-golden.py 再生基线
- 验收命令: `pytest tests/test_golden.py`

## TASK-M3-05 ✅ · 框架内容信号消噪
- 内容: 内容子串路径跳过测试文件（复用 quality.is_test_file）；声明依赖路径不受影响
- 验收命令: `pytest tests/test_frameworks.py`

## TASK-M3-06 ✅ · 收尾记录
- 内容: AGENTS/CHANGELOG 回写；A 线至此对 02/03 号项目的交付承诺全部就绪
- 验收命令: `pytest && ruff check src tests && ruff format --check src tests`

---

### 明确不做
tree-sitter AST / 圈复杂度 / 二进制 blame / 真实 25 仓库 golden 扩充（待有本地克隆）
