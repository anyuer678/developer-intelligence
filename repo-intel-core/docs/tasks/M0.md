# M0 任务卡 —— 能看见（最小可用）

> 目标：对自己全部仓库 100% 出 profile、零崩溃；语言占比与 GitHub 语言条基本一致。
> 开工仪式记录：2026-08-26 由 AI 起草本清单，按 06 手册执行；每张卡独立 commit。

---

## TASK-M0-01 ✅ · 项目骨架 ✅
- 内容: pyproject/LICENSE/.gitignore/README/AGENTS.md/docs 目录
- 验收: `pip install -e ".[dev]"` 成功; `pytest` 可运行(空收集不报错)

## TASK-M0-02 ✅ · Schema v1.0-draft
- 内容: pydantic 模型, camelCase 别名, 字段三态(Optional=None), warnings 机制
- 验收命令: `pytest tests/test_schema.py`
- 要点: model_dump(by_alias=True) 输出含 schemaVersion/topLevelDirs 等计划书字段名

## TASK-M0-03 ✅ · 规则表 + loader
- 内容: rules/languages.yaml(扩展名映射+manifest 表) rules/excludes.yaml(附录 C 清单); yaml loader 带缺文件降级
- 验收命令: `pytest tests/test_rules_loader.py`

## TASK-M0-04 ✅ · 排除器
- 内容: 默认目录/文件清单剪枝(遍历时不下钻); `.repointelignore` 支持(glob 行, `#` 注释); >2MB 文件只计数不读内容
- 验收命令: `pytest tests/test_excludes.py`

## TASK-M0-05 ✅ · 语言识别（三级信号）
- 内容: 扩展名统计(loc/files) → manifest 信号(go.mod/package.json/pyproject 等, 记入 config_files 与 monorepo 判定) → 无扩展名文件 shebang 指纹(仅读前 2KB); unknown 归桶; pct 按 loc 归一化保留 1 位
- 验收命令: `pytest tests/test_language_detect.py`

## TASK-M0-06 ✅ · 结构扫描 + 规模统计
- 内容: 顶层目录表(fileCount+role 粗判: 含 main.*→guessed-entry, 含 package.json 且有 src|app→guessed-frontend, 其余 null); metrics(totalLoc/totalFiles/largestFiles top5)
- 验收命令: `pytest tests/test_structure_metrics.py`

## TASK-M0-07 ✅ · CLI
- 内容: `repo-intel scan <path> [-o out.json] [--fail-fast] [--pretty]`; 终端摘要(名称/语言 top5/warnings); 致命错误退出码 1, warnings 仍为 0
- 验收命令: `pytest tests/test_cli.py` + 手动 `repo-intel scan tests/fixtures/demo-polyglot --pretty`

## TASK-M0-08 ✅ · 测试集固化 + 收尾
- 内容: fixtures/demo-polyglot 固化为仓库内真实目录(Go+Python+TS+Vue 混合 + node_modules 陷阱 + .repointelignore); README Quick Start 实测; CHANGELOG 建立
- 验收命令: `pytest` 全绿 + `ruff check src tests` 零告警

---

### 明确不做（防蔓延，M1 卡再议）
入口点识别 / import 解析 / 模块划分 / 依赖图 / git 元数据 / golden 快照基线 / .gitignore 解析
