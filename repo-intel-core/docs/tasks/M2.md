# M2 任务卡 —— 能读懂工程

> 目标：框架识别规则表（≥30 条）+ buildRun 三源推断 + 质量指标（热点/测试证据/TODO）。
> 计划书验收（01 §七 M2）：构建命令 ≥80% 正确；02 号 Skill 可完全基于 core 输出写文档。
> 纪律同 M1：每卡独立 commit、勾选回写、决策进 `docs/decisions.md`。

---

## TASK-M2-01 ✅ · 卡片起草与 Schema add-only 扩展 ✅
- 内容: 本文件；Schema 新增 ComplexityHotspot / TestEvidence / TodoStats，Metrics 增加三组可选字段（add-only 合规）

## TASK-M2-02 ✅ · 框架识别
- 内容: `rules/frameworks.yaml` ≥30 条规则（前端/Web/AI/ORM/测试/工具链六大类）+ `detect/frameworks.py`；信号=声明依赖(带版本) ∨ 文件 glob 计数 ∨ 代码内容子串；证据链必填
- 验收命令: `pytest tests/test_frameworks.py`

## TASK-M2-03 ✅ · buildRun 推断
- 内容: `detect/buildrun.py`——manifest scripts ∨ Makefile target ∨ CI yml `run:` 行，三源交叉；锁文件判包管理器；无显式来源时按生态给默认值并降 confidence
- 验收命令: `pytest tests/test_buildrun.py`

## TASK-M2-04 ✅ · 质量指标
- 内容: 复杂度热点启发式（long-file ≥400 行 / deep-nesting 缩进≥24空格或6 Tab，仅解析语言）；测试证据（test 文件计数 + 占比 + 测试框架名）；TODO/FIXME 字节级计数
- 验收命令: `pytest tests/test_quality.py`

## TASK-M2-05 ✅ · scanner 集成与摘要增强
- 内容: frameworks/build_run/metrics 三块填充进 profile；`scan` 摘要追加 frameworks top3 与 install/dev/test 命令行
- 验收命令: `pytest tests/test_cli.py tests/test_scan_m2.py`

## TASK-M2-06 ✅ · 收尾记录
- 内容: AGENTS/CHANGELOG/decisions 回写；G2 验收挂起为用户在真实仓库抽查
- 验收命令: `pytest && ruff check src tests && ruff format --check src tests`

---

### 明确不做（M3 再议）
git 元数据 / 月度信号包 / golden 快照基线 / tree-sitter 真复杂度（圈复杂度等 AST 级指标）
