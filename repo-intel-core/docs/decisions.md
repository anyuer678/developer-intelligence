# 决策日志（ADR-lite）

格式：`日期 | 决定 | 落选方案 | 理由 | 复盘信号`

---

- 2026-08-26 | 首发 Skill 选 Onboarding | Project Architect | 与 evocode 定位重叠 | Gate 1 数字（母目录 00 文档）
- 2026-08-26 | M0 语言识别走纯启发式+表驱动 | 直接上 tree-sitter | 性能预算与零依赖优先；AST 是增强路径不是前提 | 若 golden 校准中语言误判率 >10%，提前引入 AST
- 2026-08-26 | shebang 文件计入语言统计且计 loc | 仅识别不计行 | GitHub 语言条对脚本文件同样计数；保持口径一致 | 出现统计争议时复核
- 2026-08-26 (M1) | 模块划分采用「顶层目录聚合」而非图聚类算法 | Label Propagation 等社区发现 | YAGNI：先交付说得过的确定性结果；算法复杂度不可解释会伤害上层 LLM 叙事质量 | 若 5 仓库人工确认 <80% 通过率，升级算法
- 2026-08-26 (M1) | 外部依赖只取「根 manifest 声明」，不解析传递依赖 | 从 import 反推全量外部包 | 声明口径稳定、可信、带版本；import 反推噪音大且无版本 | 上层需要未声明依赖检测时加 diff 报告而非改口径
- 2026-08-26 (M1) | InternalDep 增加 weight 可选字段 | 不存权重 | 边次数是有价值信号（热点依赖），add-only 合规 | 无
- 2026-08-26 (M1) | Vue 文件按整文本跑 JS import 正则 | 解析 <script> 块后再提取 | import 语法唯一性强，全文匹配足够；省一个状态机 | 出现模板内误报时收紧
- 2026-08-26 (M2) | 复杂度热点用「长文件+深缩进」启发式 | AST 圈复杂度 | M2 无 AST 依赖预算；两信号零成本可解释，覆盖八成直觉场景 | 上层 LLM 反馈热点不准时引入 tree-sitter
- 2026-08-26 (M2) | buildRun 推断优先级 manifest > Makefile > CI > 生态默认 | 平权合并 | 用户最常维护 scripts 字段；默认值必须降 confidence 以示区分 | G2 抽查 <80% 时调权重
- 2026-08-26 (M2) | TODO/FIXME 用字节级计数不解码 | 解码后正则 | 编码免疫、零成本；大小写敏感口径固定并在 schema 注明 | 出现 TODO 变体需求时再扩
- 2026-08-26 (M3) | gitmeta 走 subprocess 本地只读 git 命令 | 纯 Python 读 .git 对象库 | git 命令稳定且免实现 packfile 解析；硬约束#3 澄清为禁网络/LLM，本地只读 VCS 命令合规 | 出现无 git 环境部署需求时降级路径已备（null+warning）
- 2026-08-26 (M3) | 月度信号包独立 CLI 导出，不塞进 RepoProfile | 扩展 GitMeta.signals 字段 | Profile 面向仓库快照，信号包面向时间序列，消费方与更新节奏不同；避免 Schema 膨胀 | 03 号 Timeline 若需要单文件交付再合并
- 2026-08-26 (M3) | golden 基线先覆盖三个内存 fixture，25 真实仓库待克隆后扩 | 立即克隆全部自有仓库 | fixture 快照已锁住回归面；真实仓库引入网络与磁盘成本，留到有 CI 缓存时 | M4 或开源前补齐
- 2026-08-26 (M3后) | evocode 对齐策略=分批下沉而非一次性迁移 | 全量重写/直接依赖 evocode 包 | 两仓形态不同（平台 vs CLI）；core 已覆盖部分更优；分批让每批都有测试与回归保护 | M4 候选清单见母目录 09 号审计文档
- 2026-08-26 (M3后) | 框架规则表吸收 evocode DB_KEYWORDS 为 database 类目 | 忽略数据库维度 | 数据库选型是架构报告第2节的重要事实；9 条规则零成本扩类目 | 无
- 2026-08-26 (M4) | 认知复杂度忠实移植 evocode 正则版（含嵌套深度按行首关键字累积的原版特性） | 自研精确 AST 版 | 承诺口径一致便于对照校准；AST 精化已在 tree-sitter 大件里顺路解决 | 阈值误报反馈集中时先调阈值再上 AST
- 2026-08-26 (M4) | tree-sitter 作为可选 `[arch] extra`，缺失时整体降级 null 三态且 golden 归一化剥离该输出 | 纳入运行时必装依赖 | 锁内零第三方运行时依赖纪律不破坏；CI 无 extra 也绿；正确性由专测守护 | 接 lumen 需要调用图常驻时升级为默认依赖
- 2026-08-26 (M4) | 调用图 v0.2 边界忠实移植（attribute 根段匹配类名、Go 接收者聚合、Java 字段类型映射） | 自研精确类型解析 | 与 evocode 口径一致便于对照；类型解析属 v0.3 范围 | 跨文件误连/漏连反馈集中时上精确解析
- 2026-08-26 (M4) | Maven 口径继承 evocode：仅根 pom 直接依赖，剥离 dependencyManagement/parent | 展开管理块/聚合子模块 | 防止 BOM 构件误入与版本归因错误；多模块聚合属平台场景 | 用户提出多仓需求时再议
- 2026-08-26 (M3后) | 自举验证#1：onboarding skill 对 core 自身跑扫描，发现并回修两个引擎 bug——①入口点内容信号未排除 tests/ 目录（测试文件误判为 CLI 入口）；②buildRun 在缺 requirements.txt 时仍推荐该安装命令 | 仅人工抽检 | **工具自检发现自身缺陷**：dogfood 闭环成立，证明 onboarding skill 对真实仓库有鉴别力；两个 bug 均为边界条件——fixture 覆盖不到的"正常但不期望"场景 | 核心发版前对自有仓库重跑一轮自举
- 2026-08-26 (M4) | EOL 未命中不产任何标记 | 标记 LOW 未知 | 不误报原则；前端语义由消费者解释 | 无
- 2026-08-26 (M4) | 演化热点阈值沿用 evocode 原版（HIGH=占比≥15%且新增≥300 或新增≥2000；MEDIUM=变更≥3） | 自定阈值 | 口径一致便于两仓对照校准；evidence 数组结构同步移植 | 数据规模变化时按原版注释提示调参
- 2026-08-26 (M4) | evolution 单次 numstat 全量+内存聚合 | 逐月多次 git 调用 | 与 signals 的逐月 diff 解耦，性能可控；空仓库[]与故障 None 显式区分（继承原版） | 超大仓库超时时改分页策略
