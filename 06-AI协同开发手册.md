# 06 · AI 协同开发手册 —— 让 AI 成为可靠的执行者

> 适用范围：01-04 号项目的全部开发工作
> 前提变化：**执行主体 = AI（opencode / Claude Code / dsh 等 agent），人 = 架构师 + 验收官**
> 本手册回答一个问题：怎样组织工作，AI 才能又快又稳地把这些项目做出来

---

## 一、核心认知：AI 开发的三个成败变量

| 变量 | 含义 | 本体系的对策 |
|---|---|---|
| **上下文质量** | AI 每次会话能拿到多准的项目事实 | 三层文档体系（§二）+ 仓库内 AGENTS.md（§三） |
| **验证闭环** | AI 能否在秒级~分钟级自证"我做对了" | 一键测试 + golden 锚点 + CI 门（§五） |
| **任务粒度** | 单次交给 AI 的活有多大 | 任务卡制度：一张卡 = 一个会话（§四） |

推论：**计划书写得再好，不翻译成 AI 可消费的形态就没用。** 本手册就是那层翻译规范。

## 二、三层文档体系（谁写给谁看）

```
第1层 计划书(00-05)          ← 人写给人: 方向/阶段/止损, 不进 AI 会话
        ↓ 每个 M 开始时拆解
第2层 里程碑规格 docs/tasks/M<x>.md   ← 人+AI共同拆: 一张张任务卡(§四), AI 的施工图
        ↓ 仓库建立时固化
第3层 AGENTS.md + PROJECT_GUIDE.md    ← 写给每次 AI 会话: 项目事实+硬约束(§三)
```

规则：
- 第 2 层是主战场。**里程碑开工仪式**：人提出 M 的目标 → AI 起草任务卡清单 → 人修订批准 → 逐卡执行
- 第 3 层必须保持与代码同步——收尾协议里有回写步骤（§七）
- 禁止把整份计划书塞给 AI 当上下文：太大且含大量它不需要的战略叙事

## 三、仓库标配的 AI 接入文件集

每个开源仓库根目录固定包含：

| 文件 | 服务对象 | 来源 |
|---|---|---|
| `AGENTS.md` | AI agent（opencode/Claude Code/dsh 通读） | 按 §三.1 模板手写 |
| `PROJECT_GUIDE.md` | 新人 + AI 冷启动 | **用自家 02 号 Skill 生成后人工校订**（自举） |
| `docs/tasks/M*.md` | AI 施工图 | §二 开工仪式产物 |

### 三.1 · AGENTS.md 标准模板

```markdown
# AGENTS.md — <repo>

## 项目一句话
<一句话>

## 命令
- 安装: <cmd>
- 测试: <cmd>     ← 改任何代码后必须运行
- lint:  <cmd>    ← 提交前必须零告警
- 单测某文件: <cmd 示例>

## 架构地图（≤10 行）
<目录 → 职责 一行一个>

## 当前状态
- 已完成: M0
- 进行中: M1 / 任务卡 #<id>
<每次收尾更新此节>

## 硬约束（违反即返工，无例外）
1. <例: 不得修改 schema/*.py 已有字段，只能新增>
2. <例: 不新增第三方依赖，除非任务卡明确允许>
3. <例: src/ 内禁止出现任何 LLM/网络调用>
4. 注释与文档用中文；commit 用 Conventional Commits（见 05 手册）

## 禁改区
- <路径>: <原因>。<允许的唯一变更方式>

## 完成定义
新代码有对应测试 + 全量测试绿 + lint 绿 + 本卡验收命令通过
```

要点：硬约束一栏是**从血泪里长出来的**——每踩一次坑就加一条，宁多勿漏。

### 三.2 · 自举闭环（本项目独有的优势）

```
02 号 Skill 生成 PROJECT_GUIDE.md
        │ 用于 01/03/04 仓库的 AI 冷启动
        ▼
AI 在这些仓库上的真实使用体验 = issue 反馈
        ▼
反哺 02 号 Skill 的模板与扫描质量
```

**自己的仓库就是 Skill 的第一个测试床。**

## 四、任务卡规范（AI 的施工图单元）

### 粒度标准

**一张卡 = 一个 AI 会话能完成并自验的原子工作量**（典型 30 分钟~2 小时的人类等效）。超过就拆。

### 卡片格式（docs/tasks/M1.md 中逐条排列）

```markdown
## TASK-CORE-M1-03 · import 关系提取(Python)
- 目标: 从 .py 文件提取顶层 import, 输出模块名列表
- 输入: 单文件文本
- 输出: list[ImportRecord], 定义于 schema/graph.py(已存在)
- 做法 hint: 正则优先; try/except 包裹; 失败返回空列表+warning
- 验收命令:
  pytest tests/graph/test_py_imports.py -q   # 8 个用例全绿
  python -m repo_intel scan tests/fixtures/demo-py/ --section graph
- 禁改区: schema/, rules/
- 不做清单: 相对导入解析(M1-04 的卡)、stdlib 分类
- 参考: 01 计划书 §四 dependencyGraph 字段
```

要素解析：**验收命令**是卡片的心脏——AI 完成与否由命令输出判定，不由感觉判定；**不做清单**防止 AI 顺手扩张；**禁改区**保护契约。

### 批量执行模式

简单卡片可以一次会话连做 3~5 张，条件：彼此无依赖、每张完成都跑全量测试并单独 commit。

## 五、验证闭环 —— AI 的三条生命线

| 生命线 | 内容 | 各项目落地 |
|---|---|---|
| ① 秒级本地验证 | `make check` 一条命令 = lint+test；AI 每步改动后必跑 | 全项目统一 Makefile/taskfile |
| ② 事实锚点 | 有客观基准可对比，防"看起来对" | core: golden 快照；pgi: 录制的 GitHub API 响应(VCR 式 fixture)；skill: 固定输入仓的期望文档骨架 |
| ③ CI 门 | 推送即全量复核，本地作弊无效 | 05 手册 §六基线 |

补充纪律：
- **fixture 先行**：涉及外部世界（GitHub API/LLM）的功能，先把真实响应录成 fixture 再开发。AI 对着 fixture 开发又快又稳，还省 API 费
- **LLM 层的验证特殊化**：不比对全文，比对结构（JSON schema 校验）+ 关键断言（引用了哪些证据字段）+ 人工抽检留档
- 测试可信度抽查：人每周随机挑 2 个 AI 新写的测试，确认不是永真式/mocked 到失真

## 六、人机分工与审批门

| 事项 | 归属 | 说明 |
|---|---|---|
| Schema 定稿与变更 | 🔴 人批准 | 契约级，牵一发动全身 |
| Prompt 模板改动 | 🔴 人批准 | prompt=产品（02 号尤其） |
| 规则表(yaml)新增条目 | 🟡 AI 自主 + 人抽查 | 表驱动就是为了这个 |
| 常规函数实现/重构/修 bug | 🟢 AI 自主 | 任务卡边界内 |
| README/CHANGELOG 文案 | 🟡 AI 草稿 + 人过目 | |
| golden 基线更新 | 🔴 人 review diff | 见 01 附录 F |
| 发版 | 🔴 人按检查单执行 | 05 手册 §五 |
| 决策日志记录 | 🟡 AI 提议 + 人确认 | |

原则口诀：**契约和出口归人，实现和体力活归 AI。**

## 七、会话协议

### 冷启动五步（每次新会话开头）

```
1. 读 AGENTS.md
2. 读当前里程碑 docs/tasks/M*.md 里未完成的下一张卡
3. 先跑一遍测试, 确认起点是绿的(红了先修环境再干活)
4. git log --oneline -10 了解最近发生了什么
5. 动手
```

### 收尾四件套（每张卡结束）

```
1. 全量测试绿 + lint 绿
2. 单独 commit (type(scope): 卡号摘要)
3. 若过程中发现的新约定/新坑 → 回写 AGENTS.md 或决策日志
4. 输出一句话交接: "TASK-xx 完成, 下一张是 yy"
```

### 反腐化规则

- **一张卡一个会话**为主流模式；连续作战最多 5 张卡后强制开新会话
- 会话中途 AI 开始重复犯错 = 上下文已污染，立即重开而不是继续挣扎
- 大重构永远拆成"机械变换卡"序列（如：改名→跑测试→commit），每卡独立可验证

## 八、分项目开发要点

### 01 repo-intel-core —— 表驱动是 AI 的红利

- 加语言/框架支持 = 加 yaml 规则条目 = 最适合 AI 自主完成的任务类型（🟡 抽查级）
- pydantic Schema 即机器可查的契约：AI 写消费者代码时把模型源码给它，幻觉率骤降
- golden test 是 AI 的"地面真值"：任何解析器改动立刻暴露全局影响
- 开发顺序建议：schema → fixtures → 测试 → 实现（TDD 对 AI 尤其有效，因为它需要明确的靶子）

### 02 onboarding-skill —— 自举产品

- 本身就是 AI 产品：**每个版本都在自己 25 个仓库上实测**，效果数据直接进 CHANGELOG
- Prompt 是产品本体 → 改动走🔴审批门，且改前跑固定评测集（3 个标准仓库 × 期望要点 checklist）
- 三宿主(dsh/Claude Code/opencode)各配一个冒烟脚本，发版前三绿

### 03 github-intelligence —— 用真数据养 AI

- 第一批 fixture 就是录自己账号的真实 API 响应（脱敏后入库）
- 隐私规则（§十五脱敏细则）原文抄进 AGENTS.md 硬约束——AI 不会自己想起隐私
- Timeline 算法分两半开发：切片/相似度（纯函数，golden 可测，AI 自主）；命名与校对（prompt 工程，人机共管）
- `pgi` CLI 每个命令自带 `--json` 输出——既是用户接口也是 AI Analyst 的取数接口

### 04 personal-data-os —— 契约冻结后的复制游戏

- Connector Protocol（04 §十二）冻结后，每个新连接器都是"对着 Protocol+sink 内存实现写实现"的标准作业，AI 完成度极高
- EntitySink 的内存 fake 实现先行，连接器单测全部离线
- 隐私标记逻辑（privacy: high 过滤）做成独立纯函数 + 强制测试覆盖，因为这是不能出错的部分

## 九、AI 开发反模式（踩过或预见，出现即停）

| # | 反模式 | 后果 | 对策 |
|---|---|---|---|
| 1 | 假测试（断言永真/全 mock 失真） | 绿灯假象，发布暴雷 | 验收必含真实样本；每周人工抽检测试质量 |
| 2 | 一句话让 AI"把这个功能做了" | 半成品+越界改动 | 只接受任务卡输入 |
| 3 | AI 自创架构/引入框架 | 与计划书漂移，无人能维护 | 架构地图写死在 AGENTS.md；依赖变更需审批 |
| 4 | 静默修改 schema/prompt | 消费者连环崩 | 禁改区 + diff 必看 |
| 5 | 上下文腐化后硬撑 | 越改越错 | 重开会话冷启动 |
| 6 | 无验证提交 | CI 红、基线脏 | 完成定义=命令输出，不接受"应该没问题" |
| 7 | 把计划书整份喂给会话 | 上下文被战略叙事淹没 | 只喂 AGENTS.md+当前任务卡+相关源码 |

## 十、与现有工具链的关系

| 工具 | 角色 |
|---|---|
| opencode / Claude Code | 主力开发 agent：读 AGENTS.md，按任务卡施工 |
| dsh | 生产环境宿主：02 号 Skill 的首发平台；dsh-logtimeline 经验复用于 pgi 时间表达式 |
| GitHub Actions | 独立裁判：本地验证之外的第二道门（AI 无法绕过） |
| keyvault | 所有 token 注入源，AGENTS.md 中声明"密钥只从环境变量来" |
| 本手册 | 元规范：约束的是"如何指挥 AI"，而非某个具体工具用法 |

---

## 附 · 手册生效方式

本手册不需要 AI 主动阅读全文——它的内容通过三条路径物理生效：

1. **AGENTS.md 模板**（§三.1）在每个仓库实例化 → AI 自动读到硬约束
2. **任务卡格式**（§四）由人在拆解里程碑时执行 → AI 收到的天然是合规输入
3. **收尾四件套**（§七）写进各仓 `Makefile release-note` 类脚本提示 → 形成肌肉记忆

> 手册的价值不在被阅读，在被结构化地执行。
