# 00 · 总路线图 —— Developer Intelligence 全景计划

> 作者：anyuer678（https://github.com/anyuer678）
> 性质：个人执行用计划书 · 只排阶段不排时间
> 配套文档：01 / 02 / 03 / 04 号计划书（见文末索引）

---

## 一、这条路线是什么

**一句话：围绕"让 AI 理解开发者的工作世界"，从一个小 Skill 开始，长成个人数据操作系统，最终成为常驻 Agent 的记忆与认知底座。**

### 五层结构

```
repo-intel-core            解析引擎（真资产，一切的地基）
      ↓
repo-onboarding-skill      首发 Skill（分发 + 需求验证探针）
      ↓
Personal GitHub Intelligence   核心产品（第一个完整产品形态）
      ↓
Personal Data OS           连接器架构（愿景容器，不是新项目）
      ↓
lumen                      Personal Agent Runtime（终点宿主）
```

### 逻辑链条（为什么是这个顺序）

1. **解析引擎是一切分析能力的地基。** 没有"把仓库变成结构化数据"的能力，上层全是空中楼阁。
2. **Skill 是引擎的第一个消费者，也是市场探针。** 它负责回答一个问题："这套理解代码的能力，别人要不要？" 用最低成本拿到真实反馈和开源曝光。
3. **GitHub Intelligence 把引擎整合成完整产品。** 数据边界清晰（只有 GitHub）、展示效果好、能服务自己的 yuer.dev，是天然的第二个里程碑。
4. **Data OS 是架构演进，不是另起炉灶。** 把"只接 GitHub"扩展为"接一切个人数据"，靠的是加连接器，不是重写。
5. **lumen 是终点宿主。** 它常驻 24/7、能操控整台电脑——它最缺的就是一个"懂你"的记忆层。前面所有项目的产出，最终都变成 lumen 的认知。

### 反过来读（终局倒推）

```
lumen 需要记忆 → 记忆需要统一数据模型 → 模型需要连接器喂 → 第一个连接器选 GitHub
→ GitHub 分析需要解析引擎 → 引擎先以 Skill 形态见人验证需求
```

---

## 二、四个新项目一览

| 编号 | 项目 | 性质 | 一句话定位 | 核心产出 | 服务对象 |
|---|---|---|---|---|---|
| 01 | repo-intel-core | 基础设施 | 跨语言仓库静态解析引擎 | RepoProfile JSON Schema + Python 库 + CLI | 所有上层项目 |
| 02 | repo-onboarding-skill | 开源组件 | 给任何仓库自动生成《新人入门指南》的 dsh Skill | SKILL.md + PROJECT_GUIDE.md 模板 | dsh / agent 用户，首发验证探针 |
| 03 | github-intelligence | 核心产品 | 理解个人 GitHub 世界的 AI 分析系统 | 采集器 + SQLite + Dashboard + Evolution Timeline + AI Analyst | 自己 → 开源开发者 |
| 04 | personal-data-os | 架构愿景 | 个人数字世界的连接器架构 | 统一数据模型 + 连接器框架 | 自己 → lumen |

### 项目间依赖关系

```
                    ┌─────────────────────────────┐
                    │   01 repo-intel-core         │
                    └──────┬──────────────┬───────┘
                           │              │
              （消费 RepoProfile）    （消费 RepoProfile + gitMeta）
                           ▼              ▼
        ┌──────────────────────┐   ┌──────────────────────────┐
        │ 02 onboarding-skill  │   │ 03 github-intelligence    │
        │  （独立可降级运行）    │   │  （含 stargrave 并入）     │
        └──────────┬───────────┘   └──────────┬───────────────┘
                   │                          │
                   │     GitHub 数据库 = 第一连接器
                   │                          ▼
                   │               ┌──────────────────────┐
                   └──────────────▶│ 04 personal-data-os  │
                                   └──────────┬───────────┘
                                              │  作为记忆层接入
                                              ▼
                                   ┌──────────────────────┐
                                   │       lumen          │
                                   └──────────────────────┘
```

关键依赖规则：

- **01 不依赖任何人**，可以立刻开工；
- **02 有降级模式**（内置轻量扫描），即使 01 未完成也能发布——两者可并行；
- **03 的 M0 只依赖 01 的 M0-M1 输出**，不需要等 01 全部完成；
- **04 在 03 的 M0 跑通之前不写一行代码**，只维护文档。

---

## 三、现有资产盘点与挂接

> 全部仓库见 https://github.com/anyuer678 ，此处只列与路线相关的，其余保持独立不挂接。

### 核心挂接（深度参与路线）

#### `evocode` — AI 软件体检与演化平台（Java/Spring Boot + Vue3 + FastAPI）

| 维度 | 说明 |
|---|---|
| 现状 | 已实现项目扫描、健康分析、技术债诊断、AI 修复建议与文档生成 |
| 在路线中的角色 | **01 号项目的能力母体**。它的扫描/分析核心要被抽取出来，沉淀为独立的 repo-intel-core |
| 改造方向 | 转型为 core 的一个 Web UI 消费者；平台里与"解析"无关的部分（报告渲染、用户流程）留在 evocode |
| 明确不做 | 不要继续往 evocode 里堆新分析能力——新能力一律进 core |

#### `lumen` — 个人 Agent Runtime（Go + React，24/7 常驻）

| 维度 | 说明 |
|---|---|
| 现状 | 能操控整台电脑的常驻 Agent Runtime |
| 在路线中的角色 | **路线终局宿主**。03/04 的全部产出最终以 lumen 技能 / MCP 工具 / 记忆层的形式接入 |
| 改造方向 | 保持技能接口标准化；为 Data OS 记忆层预留存储与检索接口（详见 04 号文档第九节） |

#### `reasonix-skills` — DeepSeek Harness 技能合集（纯 Markdown）

| 维度 | 说明 |
|---|---|
| 现状 | 5 个工程范式技能：快照回归 / 决策留痕 / 可换后端 / 场景分组 / 架构范式 |
| 在路线中的角色 | **02 号 Skill 的发布阵地之一**。证明了你已经跑通过"设计→发布→维护 Skill"全流程 |
| 改造方向 | 新 Skill 独立建仓（便于单独打 Star），但在此仓库 README 互挂链接 |

#### `dsh-logtimeline` — 日志时间线查询插件（Python，⭐4）

| 维度 | 说明 |
|---|---|
| 现状 | 中文自然语言时间表达式查询本地日志，已获 4 星 |
| 在路线中的角色 | **生态验证证据 + 运营参照物**。dsh 插件生态存在真实反馈，运营打法照此复刻 |
| 改造方向 | 无需改造；其"自然语言→结构化查询"的经验可复用到 03 的 AI Analyst |

#### `stargrave` — Star 仓库清理建议器（Python）

| 维度 | 说明 |
|---|---|
| 现状 | 扫描 GitHub Star，判断已死 / 值得复查 / 保留 |
| 在路线中的角色 | **已经是 GitHub Intelligence 的雏形模块**。"已死/复查/保留"的三分类逻辑直接并入 03 的 Star 资产管理功能 |
| 改造方向 | 并入后原仓库保留归档，README 指向新项目 |

### 辅助挂接（作为基础设施被使用）

| 仓库 | 角色 | 使用方式 |
|---|---|---|
| `keyvault` | 全线密钥基础设施 | GitHub Token / LLM Key 一律经 keyvault 存取，任何新项目禁止明文落盘 |
| `kb-ui` | Dashboard 前端组件来源 | 03 的 Web Dashboard 直接复用其 Vue3 组件与主题系统 |
| `yuer.dev` | 对外展示门户 | 03 的公开统计页（脱敏后）可作为 yuer.dev 的"数字花园"板块 |
| `chatez` | 第二运行宿主 | 02 号 Skill 若做成通用 Prompt+Skill 形态，chatez 可作为演示环境 |
| `voiceconsole` | MCP 经验来源 | 其 STT+意图+安全门架构可供 lumen 接入 Data OS 时参考 |
| `picren` | 未来图片连接器参考 | 图片语义命名经验可直接迁移到 04 的图片连接器 |

### 明确不挂接

`codedrill` / `codetutor-cn` / `cet6-*` / `todo-list` / `desktoppet` / `polycodehub` / `eclipse-wasteland` / `upgrademate` / `123` / `-SSM-PetStore-CMS-` 等：与路线无协同，保持独立，**不要为了"统一"强行改造它们**。它们在 03 号产品眼里只是待分析的数据而已。

---

## 四、阶段推进规则（含止损线）

> 原则：**每个阶段只有一个目标，每个阶段门口有一道可量化的闸门。闸门不过，不改目标就换目标，绝不带着失败假设硬推进下一阶段。**

### Phase 0 —— 地基 + 探针（并行两条线）

| 线 | 内容 | 完成定义 |
|---|---|---|
| A 线 | repo-intel-core 做到 M1（语言识别+入口+模块划分+依赖图） | 对自己 25 个仓库全部成功输出 RepoProfile |
| B 线 | onboarding-skill 做到可发布（含降级模式） | 发布到 GitHub + PR 进 awesome-dsh-plugin |

**Gate 1（Skill 止损线）：**
- ✅ 通过：发布后 4 周内 Star ≥ 50 **或** 出现 ≥ 3 个非本人提的 issue/PR **或** 有人晒出使用产物
- ❌ 不过：复盘原因（需求伪？分发差？产物质量低？），换备选方案（Project Architect Skill）再试一轮；两轮皆败则放弃 Skill 先行策略，直接做 03 自用版

### Phase 1 —— GitHub Intelligence 自用版

内容：03 的 M0（采集器 + SQLite + CLI 报告），先把自己的 25 个仓库完整吃透。

**Gate 2（自用黏性闸门）：**
- ✅ 通过：连续 2 周、每周主动打开 ≥ 3 次，且至少产生过一次"哦原来如此"的真实洞察
- ❌ 不过：砍掉 Dashboard 计划，收缩为纯 CLI + Timeline，把自己当唯一用户养着

### Phase 2 —— 差异化功能 + 开源

内容：Evolution Timeline + Developer Timeline（核心卖点）打磨到自己满意，然后开源宣传。

**Gate 3（卖点成立闸门）：**
- ✅ 通过：Timeline 能准确说出自己每个项目的演化阶段（人工评判 ≥ 80% 准确）；发一篇文章/帖子能带来 ≥ 50 外部访问
- ❌ 不过：说明算法或呈现不到位，回到 M2 内部迭代，**不带病开源**

### Phase 3 —— 连接器扩张（Data OS 启动）

每加一个连接器 = 一个独立小阶段，各自有验收（详见 04 文档）。规则：**同一时间只开一个连接器，上一个没达到"自用黏性"标准就不开下一个。**

### 终局合流

Data OS 具备文件 + GitHub 两个连接器后，开始向 lumen 移植记忆层，进入 Agent Runtime 合流阶段。

---

## 五、个人现状与资源盘点（开工前的自我校准）

### 技术栈匹配度

| 路线需要 | 已有证据 | 匹配度 |
|---|---|---|
| Python 工程化 | stargrave / dsh-logtimeline / picren / upgrademate 全是 Python | ✅ 强 |
| 前端 Dashboard | kb-ui（55+ 组件）、yuer.dev（Vue3+Vite） | ✅ 强 |
| Go / Runtime | lumen 在建 | 🟡 够用，路线内不新增 Go 任务 |
| tree-sitter / AST | 无直接经验 | 🔴 学习清单 L1 |
| GitHub GraphQL 分页/限流 | 无系统用过 | 🔴 学习清单 L2 |
| SQLite FTS5 / embedding | logtimeline 有查询经验，检索无 | 🟡 学习清单 L3 |

**学习清单纪律：用到才学，学时上限一周。** L1 在 core M1 前学；L2 在 03 M0 前学；L3 在 04 DS-2 前学。

### 时间预算假设

- 模式：业余投入，工作日 2~3h + 周末各一个整块上午
- 推论①：并行上限 = 2 条线（仅 Phase 0 的 A/B 线），此后永远单主线
- 推论②：任何里程碑的粒度必须能被"一个周末整块"推进至少一半，否则继续拆
- 环境注意：Windows 主力机——所有路径处理默认 UTF-8、容忍中文目录名；CI 统一用 GitHub Actions Linux runner 保证产物跨平台

---

## 六、每周执行节奏

| 时段 | 动作 |
|---|---|
| 周一~周五晚 | 主线推进（当前唯一 in-progress 的里程碑任务） |
| 周六上午（整块） | 攻坚：本周最难的一块放在这里 |
| 周日晚 | 周复盘 + 下周计划；（Phase 1 起）顺手 `pgi sync` |

### 周日自查五问（写进周报，一问一行）

1. 本周主线推进了哪个可验证产出？（贴链接/命令输出）
2. 有没有偷偷开新坑？有则当场归档或写进 backlog
3. 当前阶段的 Gate 还差哪些数字？
4. 下周唯一主线是什么？
5. 有没有值得记入决策日志的选择？

### 决策日志（ADR-lite）

每个"方向性选择"记一条，格式：`日期 | 决定 | 落选方案 | 预期信号 | 复盘日期`。
示例：`2026-08-26 | 首发 Skill 选 Onboarding 而非 Architect | Architect 与 evocode 定位重叠 | Gate 1 数字 | 发布后第 4 周`。

---

## 七、失败剧本推演（提前写好遗书）

| 剧本 | 最早预警信号 | 触发后的动作 |
|---|---|---|
| S1 · Skill 无人问津 | 发布 2 周 Star < 10 且无外部来源访问 | 提前启动 Gate 1 止损决策树，不要干等满 4 周 |
| S2 · core 完美主义 | M0 超期且 Schema 一周内改动 > 2 次 | Schema 冻结 7 天，先交付能跑的丑版本 |
| S3 · 全面开花 | 同时 ≥ 3 个仓库存在未提交改动 | 回到 Gate 制度：只留主线，其余显式挂起并记录 |
| S4 · 热情衰减（最常见） | 连续两个周日没写周报 | 允许降速但不允许换方向；缩小当前里程碑到"本周末能完成"的粒度 |

---

## 八、统一技术原则（所有项目强制遵守）

1. **Local-first**：SQLite 单文件数据库起步；无服务端、无账号体系、离线可用。
2. **LLM Provider 可插拔**：所有 AI 调用走统一抽象接口。默认 DeepSeek（便宜），支持 Ollama 本地模型（零成本离线），预留 OpenAI 兼容协议。**用户跑一次全量分析的 API 成本必须可控可预估。**
3. **引擎与皮分离**：core 不含任何 LLM 逻辑；LLM 总结永远发生在消费者一侧（Skill / Intelligence / lumen）。保证核心可以被完全离线测试。
4. **密钥安全**：一律经 keyvault 存取；日志与数据库中永不出现明文 token。
5. **渐进披露**：每个产品先 CLI 后 UI；CLI 是稳定接口，UI 是锦上添花。
6. **Schema 版本化**：RepoProfile、SQLite 表结构、Timeline JSON 都带版本号，向前兼容有明确策略。
7. **YAGNI 红线**（出现即拒绝）：自建同步服务、上重型图数据库、做多用户 SaaS、做移动端、微服务拆分、追求"全覆盖所有语言框架"。
8. **AI 可验证优先**：执行主体是 AI——每个里程碑必须附带机器可执行的验收命令与事实锚点（fixture / golden）；文档分层，计划书写给人看，AGENTS.md 和任务卡写给 AI 用。协作规范见 [06-AI协同开发手册.md](./06-AI协同开发手册.md)。

---

## 九、风险总表

| 风险 | 影响 | 对策 | 归属文档 |
|---|---|---|---|
| 范围失控（尤其 Data OS） | 全线烂尾 | Data OS 定义为架构而非项目；一次一个连接器 | 00 §四 |
| 同质化竞争 | Skill/产品无人问津 | 卖点锁定 Evolution Timeline（无人做好）；Gate 量化止损 | 03 §三 |
| GitHub API 速率限制 | 采集慢/中断 | GraphQL 批量 + 增量同步 + 条件请求缓存 | 03 §五 |
| LLM 成本失控 | 用户劝退/自己肉疼 | provider 可插拔 + 分级调用（启发式优先，LLM 只做汇总） | 01/03 |
| 私有仓库隐私 | 心理负担/泄露 | 默认只采公开数据；私有仓库显式 opt-in 且本地加密 | 03 §九 |
| 多项目并行精力分散 | 全部半成品 | 严格 Gate 制度；同一时间主线只有一条（A/B 并行仅限 Phase 0） | 00 §四 |
| dsh 生态过小 | Skill 曝光不足 | Skill 做成 harness 无关的通用形态，dsh 只是首发渠道之一 | 02 §七 |

---

## 十、文档索引

| 文件 | 内容 |
|---|---|
| [01-repo-intel-core-计划书.md](./01-repo-intel-core-计划书.md) | 解析引擎：数据模型 Schema、语言识别策略、模块划分算法、M0-M3 |
| [02-repo-onboarding-skill-计划书.md](./02-repo-onboarding-skill-计划书.md) | 首发 Skill：双模式设计、PROJECT_GUIDE 模板、发布运营清单、Gate 1 |
| [03-github-intelligence-计划书.md](./03-github-intelligence-计划书.md) | 核心产品：采集器、SQLite Schema、Timeline 算法、AI Analyst、竞品差异化 |
| [04-personal-data-os-计划书.md](./04-personal-data-os-计划书.md) | 架构愿景：统一数据模型、连接器规格、知识图谱演进、与 lumen 合流 |
| [05-工程规范手册.md](./05-工程规范手册.md) | 全项目通用：README 模板、commit/CHANGELOG 规范、发版检查单、CI 基线、跨仓库依赖策略 |
| [06-AI协同开发手册.md](./06-AI协同开发手册.md) | AI 执行时代的三层文档体系、AGENTS.md 模板、任务卡规范、验证闭环、人机分工审批门、会话协议 |
| [07-进度日志.md](./07-进度日志.md) | 跨仓库执行记录（倒序）：各线里程碑、证据链接、挂起人工项、全局状态快照 |
