# M0(P0 准备) 任务卡 —— Gate 1 解锁前的合规开发

> 时序锁（03 计划书 §九）：Gate 1 前"最多做到 M0 准备（schema 设计），不写采集代码"。
> 本清单全部落在允许范围内；解锁后直接续排正式 M0 卡。

---

## TASK-P0-01 ✅ · 骨架与记录体系 ✅

## TASK-P0-02 ✅ · 实体 ID 契约（04 DS-0 交付物）
- 内容: ids.py —— build/parse/validate `{connector}:{type}:{native_id}`；content_hash(NFC+CRLF 归一后 sha256)
- 验收命令: `pytest tests/test_ids.py`

## TASK-P0-03 ✅ · SQLite Schema v1 草案
- 内容: db/schema.sql —— repos/commits/issues/releases/my_stars/repo_languages/dependencies/sync_state + FTS5(commits/issues/repos) 外部内容表+触发器 + schema_meta 版本表
- 验收命令: `pytest tests/test_db.py`

## TASK-P0-04 ✅ · db 工具层
- 内容: connect(WAL+foreign_keys) / init_db 幂等 / user_version 写入；`:memory:` 可用
- 验收命令: 同上

## TASK-P0-05 ✅ · 收尾与解锁清单
- 内容: 回写记录；列出 Gate 1 解锁后 M0 正式卡的前置输入清单（token 来源 keyvault、GraphQL 查询骨架已在 03 计划书 §十一）
- 验收命令: `pytest && ruff check src tests`

---

### 明确不做（时序锁内禁止）
collector / GraphQL 请求代码 / token 读取 / Dashboard / Timeline 算法实现


---

## 锁内追加卡（Timeline 先行，2026-08-26）

## TASK-T01 · timeline.py 算法引擎 ✅
- 内容: 稀疏月合并 / 特征向量 / 余弦边界(含日历缺口强制切) / 小阶段并邻 / 三渲染器
- 验收: \pytest tests/test_timeline.py## TASK-T02 · CLI timeline 子命令 + schema.timeline_analysis 表 ✅
## TASK-T03 · 跨仓集成冒烟（core signals → pgi timeline）✅
## TASK-T04 · Analyst 检索层 ✅
- 内容: analyst.py 分词清洗/FTS 安全 MATCH/中文时间解析 v1/上下文组装；CLI ask 子命令
- 验收: \pytest tests/test_analyst.py\（16 项）
## TASK-T05 · lumen 五工具层 + MCP 调度器 ✅
- 内容: tools.py 五工具 / mcp_server.py 最小 JSON-RPC / cli mcp 子命令
- 验收: \pytest tests/test_tools_mcp.py\（11 项）
