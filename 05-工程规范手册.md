# 05 · 工程规范手册 —— 全项目通用

> 适用范围：01 / 02 / 03 号开源项目及未来所有新仓库
> 性质：强制规范。与某项目计划书冲突时，以该项目计划书为准并回来修订本手册
> 目标：让每个仓库看起来出自同一位靠谱工程师之手

---

## 一、开源仓库标配文件清单

| 文件 | 必须？ | 规范 |
|---|---|---|
| README.md | ✅ | 结构见 §二 |
| LICENSE | ✅ | **全线 GPL-3.0**（2026-08-26 用户决定：README/License 格式对齐 evocode，含免责声明节） | 工具/库默认 MIT；含"工程范式/方法论"内容沿用 GPL-3.0（与 reasonix-skills 一致） |
| CHANGELOG.md | ✅ | 格式见 §三；v0.1 发布起维护 |
| .github/ISSUE_TEMPLATE | ✅ | bug 报告 + 功能建议两个模板，bug 模板强制带环境信息字段 |
| .github/workflows/ci.yml | ✅ | 基线见 §六 |
| SECURITY.md | 🔒 含密钥/token 处理的仓库必须（keyvault、pgi） | 私密披露邮箱 + 响应时限承诺 |
| CONTRIBUTING.md | 可选 | v1.0 前用 README 一节代替即可 |
| .gitignore | ✅ | 各语言标准模板起步 |

## 二、README 黄金结构

```markdown
[徽章行: license | 版本 | 语言/运行时 | CI 状态]
[GIF 或截图 —— 第一屏, 无声 8~15s, 展示核心价值]
# 项目名
一句话定位(中文 + English)
## Why        — 解决什么痛, ≤3 行
## Quick Start — 复制粘贴即可跑通的最小路径(≤3 条命令)
## 核心概念   — ≤5 个名词, 每个一句话
## 使用示例   — 真实输出折叠块
## 与 X 的关系 — 和同类工具的差异表(诚实, 不贬低竞品)
## Roadmap / FAQ / 致谢
```

铁律：**Quick Start 必须在新机器上实测通过后才允许发布**；README 里每个命令都进 CI 冒烟测试。

## 三、版本与变更记录

- 遵循 SemVer；`0.x` 阶段 minor 允许破坏性变更（core 的 schema 就处于此阶段），`1.0` 起承诺
- CHANGELOG 采用 Keep a Changelog 格式：

```markdown
## [Unreleased]
### Added / Changed / Fixed / Removed
## [0.1.0] - 2026-09-XX
- 首个公开版本: ...
```

- 发版时把 Unreleased 内容落为版本号段落，**禁止空版本发版**

## 四、Commit 规范（Conventional Commits）

```
<type>(<scope>): <祈使句摘要, ≤50 字>

feat(collector): 支持 GraphQL 游标断点续传
fix(timeline): 合并小于两个月的碎片阶段
docs!: RepoProfile schema v1.0 定稿        ← 破坏性变更加 !
类型: feat fix docs refactor test chore ci release
```

- scope 用模块名，与代码目录一致
- 一个 commit 只做一件事；`git commit` 前跑本地 lint/test（§六）

## 五、发版检查单（每个 Release 逐项打勾）

```
□ CHANGELOG 已更新且无空段
□ 版本号符合 SemVer; tag = 代码内 __version__ 一致
□ CI 全绿 (lint + test + golden)
□ README 的 Quick Start 在干净环境实测通过
□ 若是破坏性变更: 迁移说明写入 CHANGELOG 同段
□ GitHub Release 页发布 notes(从 CHANGELOG 复制), 附产物(GIF 不强求)
□ 发版后 24h 内盯 issue
```

## 六、CI 基线（GitHub Actions）

```yaml
# 所有仓库统一骨架要点(非全文):
on: [push, pull_request]
jobs:
  lint:    ruff check . && ruff format --check .     # Python 仓
           # 或 eslint/prettier                       # TS 仓
  test:    pytest -q --cov=src --cov-fail-under=60   # Python 仓
  golden:  python scripts/run_golden.py              # 仅 01 core
  smoke:   python scripts/smoke_readme.py            # 校验 README 内命令可解析
runner: ubuntu-latest; 缓存 pip/pnpm; Windows 兼容性问题不阻塞合并但记 issue
```

覆盖率红线 60% 是底线不是目标——core 这类引擎目标 ≥80%。

## 七、Issue / PR 运营规范

- **首响应 ≤ 24h**（哪怕只是"收到，周中回"）；修复 SLA 见各项目计划书
- 回复模板精神：先感谢 → 复述问题确认理解 → 要最小复现 → 给 workaround 或时间表
- 关闭 issue 必须留一句结论（修了什么 / 为何不做）
- 外部 PR：48h 内 review；小修直接合，风格问题代改而不是打回——**对早期项目，贡献者比代码贵**
- 每季度清一次 stale issue（打 `stale` 标签两周后关闭）

## 八、跨仓库依赖策略

```
repo-intel-core (0.x)          ← semver 宽松期: minor 可破坏, patch 只修
   ▲                ▲
   │ pip install     │ pip install
onboarding-skill      github-intelligence
   锁定: >=0.3,<0.4       锁定: >=0.3,<0.4
```

- core 发布破坏性变更（minor 升级）时，**同一 PR 内同步升级两个消费者的锁版本并跑通其测试**，不允许消费者被动坏掉
- keyvault 对外只承诺 CLI 接口稳定；库接口随时可变

## 九、安全基线

1. pre-commit 钩子扫描疑似密钥模式（sk-、ghp_、AKIA…）后再提交；keyvault 引用以环境变量注入，代码零硬编码
2. 依赖每月 `pip-audit` / `pnpm audit` 一次，高危 72h 内升级
3. 日志脱敏：任何 logger 输出前过 token 正则清洗
4. 数据类项目（pgi/data-os）：默认只采公开数据，private opt-in 开关默认关
5. 发现安全问题的外部报告走 SECURITY.md 私密渠道，**不在公开 issue 讨论细节**

---

## 附 · 本手册自身的演进

- 规范变更走 PR（哪怕只有自己一个贡献者），留下决策痕迹
- 每个项目 Gate 通过时回顾一次本手册：哪些条款形同虚设就删掉，保持少而真
